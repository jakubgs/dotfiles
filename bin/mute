#!/bin/bash

set -x

MASTER_MUTE="Master Playback Switch"
DEVICE_NAME="pulse"
CARD_NUMBER=0

/usr/bin/amixer -q -D $DEVICE_NAME set $MASTER_MUTE toggle
/usr/bin/amixer -q -c $CARD_NUMBER set $MASTER_MUTE toggle
