#!/bin/bash

MASTER_MUTE="Audigy Analog/Digital Output Jack"
CARD_NUMBER=0

/usr/bin/amixer -c "$CARD_NUMBER" sget "$MASTER_MUTE" | grep off
if [[ $? -eq 0 ]]; then
	/usr/bin/amixer -q -c "$CARD_NUMBER" set "$MASTER_MUTE" on
else
	/usr/bin/amixer -q -c "$CARD_NUMBER" set "$MASTER_MUTE" off
fi
