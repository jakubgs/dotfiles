#!/bin/bash

FONT="xft:Terminus:size=10:lang=pl"
BOLD_FONT="xft:Terminus:size=10:lang=pl:style=bold"

urxvtc -xrm "urxvt*font: ${FONT}" -xrm "urxvt*boldFont: ${BOLD_FONT}" -xrm "urxvt*imFont: ${FONT}" -xrm "urxvt*italicFont: ${FONT}"
