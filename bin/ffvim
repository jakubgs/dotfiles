#!/bin/bash

LINES=$1
COLUMN=$2
shift 2

#echo "^[]0;ffvim^G"

urxvtc -title 'ffvim' \
        -e nvim \
        -f +'set titlestring=ffvim' \
        +"set ft=markdown" \
        +$LINES \
        +"sil! call cursor(0, $COLUMN)" $@
